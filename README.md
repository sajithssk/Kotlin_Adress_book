# Kotlin_Adress_book

This project is a Kotlin/Java Spring Boot application providing a command-line interface to manage address and phone number data via JPA persistence. It uses Gradle for build automation and follows a modular adapter pattern (e.g. JPA entities such as PhoneNumberEntity). 
